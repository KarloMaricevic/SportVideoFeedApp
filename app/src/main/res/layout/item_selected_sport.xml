<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/item_spinner_constraint_layout"
    android:layout_height="20dp"
    android:layout_width="150dp"
    android:orientation="horizontal"
    tools:viewBindingIgnore="true"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:contentDescription="@string/sport_image_content_description"
        android:id="@+id/sport_image_image_view"
        android:layout_height="0dp"
        android:layout_width="20dp"
        app:layout_constraintBottom_toBottomOf="@id/item_title"
        app:layout_constraintEnd_toStartOf="@id/item_title"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="@id/item_title"
        tools:src="@drawable/ic_football">
    </ImageView>

    <TextView
        android:id="@+id/item_title"
        android:layout_height="0dp"
        android:layout_width="wrap_content"
        android:singleLine="true"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintStart_toEndOf="@id/sport_image_image_view"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Football">
    </TextView>

</androidx.constraintlayout.widget.ConstraintLayout>
